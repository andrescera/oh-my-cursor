Task 18 — Worktree docs-to-reality alignment + notepad path validation
=======================================================================

A) PHANTOM FEATURE CHECK: worktree_path in hooks source
-------------------------------------------------------
Command:
  grep -rn "worktree_path" hooks/ --include="*.ts" | grep -v test

Result: 0 matches (exit 1).

Conclusion: No hook or daemon source reads `worktree_path` from boulder.json
(or anywhere else). The audit's "worktree_path documented but unimplemented"
item refers to a feature that is neither implemented NOR currently claimed in
the shipped docs (see B). No code changes required; nothing to remove.


B) start-work.md "hook-side worktree tracking" claim
----------------------------------------------------
Command:
  grep -niE "worktree|boulder\.json|boulderState" commands/start-work.md

Result: 0 matches (exit 1).

Conclusion: commands/start-work.md does NOT contain any worktree_path /
boulder.json / hook-side worktree-tracking claim. The task's line reference
(start-work.md:40-60 "worktree_path/boulder.json claims") is STALE relative to
the current file: lines 40-60 describe per-conversation plan state in
`.cursor/state/active-plan-{conversationId}.json` only. The doc already matches
implementation reality (manual plan-state tracking, no worktree tracking), so
no rewrite was needed. Expected outcome "start-work.md no longer claims
hook-side tracking" is satisfied as-is.

The actual worktree workflow is documented in commands/worktree.md and is
entirely manual / Cursor-native (git worktree + new window), with no hook
involvement — consistent with the code.


C) worktrees.json DETERMINATION: KEEP (Cursor-native)
-----------------------------------------------------
Determination: worktrees.json is a CURSOR-NATIVE worktree setup config, NOT
oh-my-cursor dead code. KEEP it and its installer copy steps.

Evidence:
  - docs/cursor/09-cli.md:211-212
      "--skip-worktree-setup  Skip running worktree setup scripts from
       .cursor/worktrees.json (default: false)"
  - docs/cursor/15-settings-and-flags.md:81
      lists `cursor.worktreesSetup` as a native Cursor setting.
  - File shape `{ "setup-worktree": [ ...shell strings... ] }` matches Cursor's
    documented worktree-setup-script config schema.

How Cursor uses it: when Cursor creates a git worktree (agent --worktree, the
/worktree command, /best-of-n), it runs the `setup-worktree` script entries in
the new worktree unless --skip-worktree-setup is passed. The shipped script
copies the workspace `.cursor/` directory into the fresh worktree so
oh-my-cursor rules/plans/state are present there.

Consumers (grep -rn "worktrees\.json" . --include=*.sh/*.ps1/*.ts/*.md, no node_modules):
  - docs/cursor/09-cli.md:212        (Cursor CLI reference — native usage)
  - install.sh:501                   (installer copy step — KEPT)
  - install.ps1:587                  (installer copy step — KEPT)
  - .omo/plans/...                   (this remediation plan)

Action taken: documented purpose in ARCHITECTURE.md ("worktrees.json
(Cursor-native)" subsection under Configuration). install.sh / install.ps1 copy
steps left intact. File NOT removed.


D) NOTEPAD PATH VALIDATION GUARD
--------------------------------
File: hooks/handlers/sisyphus-junior-notepad.ts
Change: before deriving notepad paths from activePlan.path, validate the plan
file exists under the daemon's project root:
    existsSync(isAbsolute(path) ? path : join(projectRoot, path))
If missing -> console.warn + return {} (skip notepad advisory; no dirs created).
When projectRoot is "" (unknown) the guard is intentionally non-blocking to
preserve prior behavior (cannot validate).

Regression test: see .omo/evidence/task-18-notepad-guard.txt (5/5 pass).
