Task 9 — Zombie activePlan + boulder auto-restart KILLED
=========================================================
Date: 2026-06-11
Command: cd hooks && bun test handlers/continuation-handlers.test.ts -t "zombie"

FIX SUMMARY
-----------
- Added durable tombstone field `continuationStoppedAt: string | null` to
  ConversationState (DURABLE in types.ts + state-partition.ts + Zod schema with
  .default(null) for backward-compat load of pre-tombstone records).
- /stop-continuation and idle-deactivation (and boulder wallclock) now call
  clearContinuationDurably(): activePlan=null, boulderState=null,
  continuationStoppedAt=ISO, then markDirty(convId) + forceFlush() (write-through,
  SYNCHRONOUS writeFileAtomic — NOT the debounced save()).
- Boulder auto-restart guard in /stop: re-creates boulderState ONLY when
  `activePlan` exists AND `continuationStoppedAt === null`. /start-work clears the
  tombstone so a legitimately reactivated plan can resume.

SCENARIO 1 — /stop-continuation -> restart -> NO resurrection
-------------------------------------------------------------
The cleared plan + tombstone are persisted write-through; after rehydrating from
disk (simulated daemon restart) the next /stop returns noop (no boulder).

[oh-my-cursor][beforeSubmitPrompt] convId=zombie-stop-... msgHead=/stop-continuation
(pass) durable continuation tombstone (task-9 zombie resurrection prevention) > stop then restart does not resurrect the boulder loop (cleared plan + tombstone persisted write-through)

On-disk after write-through: activePlan=null, continuationStoppedAt!=null
After restart rehydrate: activePlan=null, continuationStoppedAt!=null
/stop after restart -> result {} (no followup, boulderState stays null)

SCENARIO 3 — idle-deactivation -> restart -> NO resurrection
------------------------------------------------------------
[oh-my-cursor][/stop] conversation=zombie-idle-... | activePlan=true | toolCallDelta=0 | consecutiveZeroDeltas=3   (idle cap reached -> clearContinuationDurably)
[oh-my-cursor][/stop] conversation=zombie-idle-... | activePlan=false | toolCallDelta=4 | consecutiveZeroDeltas=0
[oh-my-cursor][/stop] RESULT=noop reason=default
(pass) durable continuation tombstone (task-9 zombie resurrection prevention) > idle-deactivation then restart does not resurrect the boulder loop

RESULT
------
 4 pass
 0 fail
(stop->restart, crash->restart resume, idle->restart, /start-work clears tombstone)
